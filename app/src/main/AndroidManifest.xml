<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.abdul.sharetaxi">
    <uses-permission android:name="android.permission.CAMERA"  android:required="true" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"  android:required="true" />
   <uses-permission android:name="android.permission.MEDIA_CONTENT_CONTROL" android:required="true"
       tools:ignore="ProtectedPermissions" />
       tools:ignore="ProtectedPermissions" />

       tools:ignore="ProtectedPermissions" />
    <uses-feature android:name="android.hardware.camera"
        android:required="true" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginwithEmail" />
        <activity android:name=".Signup" />
        <activity android:name=".SignupOption" />
        <activity android:name=".PhoneAuthentication" />
        <activity android:name=".LoginwithPhone" />
        <activity android:name=".LinkphonewithCredential" />
        <activity
            android:name=".MapScreen"
            android:label="@string/title_activity_map_screen"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>